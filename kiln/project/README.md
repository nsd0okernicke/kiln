# Project Template

Everything here is copied into `<project>/kiln/project/` when you run `kiln.ps1 -Init` / `kiln.sh init`. Edit your project's own copy freely — editing files here only changes the starting point for future init runs, not any already-scaffolded project.
