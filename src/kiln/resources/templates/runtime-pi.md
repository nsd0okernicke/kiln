# Runtime Configuration

- **Role**: `{{ROLE}}`
- **Branch**: `{{BRANCH}}`
- **Worktree**: `{{WORKTREE}}`
- **Message database**: `{{DB_PATH}}`
- **Queue access**: use Kiln's public `kiln task`, `kiln send`, and `kiln retry` commands. Pi provider credentials and model configuration remain in the user's Pi configuration and must never be copied into this project.
